# 个人博客系统

一个功能完整的个人博客系统，前端使用Vue3 + Element Plus，后端使用Spring Boot + Kotlin + Gradle。

## 功能特性

### 前端功能
- 🎨 **满屏自我介绍**：首屏展示腾讯安卓客户端开发的个人介绍
  - 渐变背景设计
  - 头像展示（带浮动动画）
  - 技能标签展示
  - 滚动指示器引导用户查看博客
- 📝 **博客列表**：滚动查看博客文章
  - 时间线样式设计
  - 文章卡片展示（标题、摘要、封面图）
  - 悬停动效
- 🔍 文章搜索功能
- 📖 文章详情页面（支持Markdown渲染）
- 👀 文章浏览量统计
- 📱 响应式设计（适配移动端）

### 后台管理系统
- 🔐 **管理员登录认证**
  - JWT Token认证机制
  - 支持多个管理员账号
  - 密码BCrypt加密存储
  - 后台入口完全隐藏（无任何可见入口）
- 🛡️ **路由守卫保护**
  - 所有后台路由需要登录才能访问
  - 未登录自动跳转到登录页
  - Token过期自动退出
- 📋 文章列表管理
- ✏️ **Markdown编辑器**（实时预览）
  - 📋 **智能粘贴功能**：
    - 粘贴剪切板图片（如截图）自动上传
    - 粘贴包含外部图片链接的 Markdown 文本时自动下载并替换为本站链接
    - 自动跳过已是本站的图片链接
    - 后端下载失败时自动尝试前端下载（支持需要登录的网站）
    - 图片有效性验证（通过文件头魔数检测，防止下载损坏文件）
- 🖼️ 图片上传功能
- 📤 文章发布/草稿管理
- 🗑️ 文章删除功能
- 📊 **访问日志查看**
  - 记录所有访问请求（IP、URL、时间、响应状态等）
  - 支持按IP或URL搜索
  - 分页浏览和批量管理

### 后端功能
- RESTful API接口
- MySQL数据库存储
- JWT身份认证
- 访问日志记录（拦截器）
- 图片上传和存储
- 分页查询支持
- 文章状态管理

## 技术栈

### 前端
- Vue 3
- TypeScript
- Element Plus
- Vue Router
- Axios
- Marked (Markdown解析)
- Highlight.js (代码高亮)
- Vite

### 后端
- Spring Boot 3.2.0
- Kotlin 1.9.20
- Spring Data JPA
- Spring Security
- JWT (JSON Web Token)
- Gradle Kotlin DSL
- MySQL 8.0

## 项目结构

```
.
├── frontend/                 # 前端项目
│   ├── src/
│   │   ├── api/             # API接口
│   │   ├── router/          # 路由配置（含路由守卫）
│   │   ├── views/           # 页面组件
│   │   │   ├── admin/       # 后台管理页面
│   │   │   │   ├── PostList.vue      # 文章列表
│   │   │   │   ├── PostEditor.vue    # 文章编辑器
│   │   │   │   └── AccessLogs.vue    # 访问日志
│   │   │   ├── Home.vue     # 首页（自我介绍+博客列表）
│   │   │   ├── PostDetail.vue  # 文章详情
│   │   │   ├── Login.vue    # 登录页面
│   │   │   └── Admin.vue    # 后台布局
│   │   ├── App.vue          # 根组件
│   │   └── main.ts          # 入口文件
│   ├── index.html
│   ├── package.json
│   └── vite.config.ts
│
├── src/                      # 后端项目（Kotlin）
│   └── main/
│       ├── kotlin/com/blog/
│       │   ├── entity/      # 实体类
│       │   │   ├── BlogPost.kt
│       │   │   ├── BlogImage.kt
│       │   │   ├── AccessLog.kt
│       │   │   └── AdminUser.kt
│       │   ├── repository/  # 数据访问层
│       │   ├── service/     # 业务逻辑层
│       │   ├── controller/  # 控制器
│       │   ├── config/      # 配置类
│       │   │   ├── WebConfig.kt
│       │   │   └── SecurityConfig.kt
│       │   ├── interceptor/ # 拦截器
│       │   │   └── AccessLogInterceptor.kt
│       │   ├── util/        # 工具类
│       │   │   └── JwtUtil.kt
│       │   └── BlogApplication.kt
│       └── resources/
│           ├── application.yaml           # 主配置（仅指定激活环境）
│           ├── application-example.yaml   # 示例配置（模板）
│           ├── application-imac.yaml      # iMac 开发环境配置
│           ├── application-macbook.yaml   # MacBook 开发环境配置
│           └── application-prod.yaml      # 生产环境配置
│
├── build.gradle.kts          # Gradle配置
├── settings.gradle.kts
├── gradle.properties
├── Dockerfile                # Docker配置
├── init-admin.sql            # 初始化管理员账号
└── README.md
```

## 快速开始

### 前端开发

```bash
cd frontend
npm install
npm run dev
```

前端将运行在 http://localhost:3000

### 后端开发

1. 确保MySQL数据库已启动
2. 配置环境（见下方"环境配置"部分）
3. 运行Spring Boot应用

```bash
./gradlew bootRun
```

后端将运行在 http://localhost:8080

### 环境配置

项目使用 Spring Boot 的多环境配置机制，配置文件位于 `src/main/resources/` 目录下：

| 配置文件 | 用途 | 是否提交到 Git |
|---------|------|---------------|
| `application.yaml` | 主配置，仅指定激活的环境 | ✅ 是 |
| `application-example.yaml` | 示例配置模板 | ✅ 是 |
| `application-imac.yaml` | iMac 开发环境 | ❌ 否（已在 .gitignore） |
| `application-macbook.yaml` | MacBook 开发环境 | ❌ 否（已在 .gitignore） |
| `application-prod.yaml` | 生产环境 | ❌ 否（已在 .gitignore） |

#### 配置步骤

1. 复制 `application-example.yaml` 为你需要的环境配置文件（如 `application-imac.yaml`）
2. 修改配置文件中的数据库连接信息和图片上传路径
3. 修改 `application.yaml` 中的 `spring.profiles.active` 或使用环境变量指定环境

#### 切换环境的方式

**方式一：修改 application.yaml**
```yaml
spring:
  profiles:
    active: imac  # 可选: example, imac, macbook, prod
```

**方式二：运行时指定**
```bash
# Gradle 命令行参数
./gradlew bootRun --args='--spring.profiles.active=imac'

# 或设置环境变量
export SPRING_PROFILES_ACTIVE=imac
./gradlew bootRun
```

**方式三：IDE 配置**

在 IntelliJ IDEA 的 Run Configuration 中设置 VM options:
```
-Dspring.profiles.active=imac
```

#### 配置项说明

每个环境配置文件包含以下配置项：

```yaml
server:
  port: 8080                    # 服务端口

spring:
  datasource:
    url: jdbc:mysql://...       # 数据库连接URL
    username: root              # 数据库用户名
    password: xxx               # 数据库密码
  jpa:
    hibernate:
      ddl-auto: update          # 自动建表策略
    show-sql: true              # 是否打印SQL

upload:
  image:
    path: uploads/images/       # 图片上传保存路径（相对或绝对路径）

logging:
  level:
    root: INFO
    io.github.lugf027.blog: DEBUG
```

### 数据库表结构

系统会自动创建以下表：

1. `blog_post` - 博客文章表
   - id, title, content, summary, cover_image
   - author, status, view_count
   - created_at, updated_at, published_at

2. `blog_image` - 图片表
   - id, file_name, file_path, file_size
   - content_type, created_at

3. `access_log` - 访问日志表
   - id, ip_address, request_url, request_method
   - user_agent, referer, access_time
   - response_status, response_time

4. `admin_user` - 管理员用户表
   - id, username, password, nickname, email
   - enabled, created_at, last_login_at

### 初始化管理员账号

首次使用需要初始化管理员账号：

```bash
# 方式1：执行SQL脚本
mysql -h 11.142.154.110 -u with_jxkywmtimbgeuddj -p n6q4bxul < init-admin.sql

# 方式2：通过API注册（首次使用）
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123","nickname":"管理员"}'
```

**默认管理员账号：**
- 用户名：`admin`
- 密码：`admin123`

**⚠️ 重要：首次登录后请立即修改密码！**

## API接口

### 认证接口

- `POST /api/auth/login` - 管理员登录
- `POST /api/auth/register` - 注册管理员
- `POST /api/auth/validate` - 验证Token

### 文章接口

- `GET /api/posts` - 获取所有文章（分页）
- `GET /api/posts/published` - 获取已发布文章（分页）
- `GET /api/posts/{id}` - 获取文章详情
- `GET /api/posts/{id}/view` - 查看文章（增加浏览量）
- `POST /api/posts` - 创建文章 🔒
- `PUT /api/posts/{id}` - 更新文章 🔒
- `PUT /api/posts/{id}/publish` - 发布文章 🔒
- `DELETE /api/posts/{id}` - 删除文章 🔒
- `GET /api/posts/search` - 搜索文章

### 图片接口

- `POST /api/upload` - 上传图片 🔒
- `POST /api/upload/from-url` - 从URL下载图片并上传 🔒
- `POST /api/upload/from-urls` - 批量从URL下载图片并上传 🔒
- `GET /api/images/{filename}` - 获取图片

### 访问日志接口 🔒

- `GET /api/admin/access-logs` - 获取所有访问日志
- `GET /api/admin/access-logs/by-ip` - 按IP查询日志
- `GET /api/admin/access-logs/by-url` - 按URL查询日志
- `DELETE /api/admin/access-logs/{id}` - 删除单条日志
- `DELETE /api/admin/access-logs/clear` - 清空所有日志

🔒 表示需要管理员登录认证

## 使用说明

### 访问后台管理

**⚠️ 重要：后台管理入口已完全隐藏，首页没有任何入口链接或按钮**

访问方式：
1. 直接在浏览器地址栏输入：`http://localhost:3000/login`
2. 输入管理员账号密码
3. 登录成功后自动跳转到后台管理系统

**安全特性：**
- 后台入口完全隐藏，普通用户无法发现
- 所有后台路由都有路由守卫保护
- 未登录用户访问后台会自动跳转到登录页
- JWT Token认证，确保安全性

### 创建文章

1. 进入后台管理系统
2. 点击"新建文章"
3. 填写标题、作者、摘要等信息
4. 使用Markdown编辑器编写内容
5. 可以上传封面图和插入图片
6. 保存为草稿或直接发布

### 查看访问日志

**系统会自动记录所有访问请求，包括：**
- 访问时间
- 访问者IP地址
- 请求URL
- 请求方法（GET/POST等）
- User Agent（浏览器信息）
- Referer（来源页面）
- 响应状态码
- 响应时间（毫秒）

**查看方式：**
1. 登录后台管理系统
2. 点击左侧菜单"访问日志"
3. 可以查看所有访问记录的详细信息
4. 支持按IP地址或URL搜索过滤
5. 支持分页浏览
6. 可以删除单条日志或清空所有日志

**应用场景：**
- 安全审计：追踪可疑访问
- 流量分析：了解访问趋势
- 问题排查：定位异常请求
- 用户行为分析：了解用户访问路径

### Markdown编辑器

编辑器支持以下功能：
- 实时预览
- 工具栏快捷按钮（加粗、斜体、标题、列表等）
- 图片上传和插入
- 代码高亮
- 表格、引用等Markdown语法

## 部署

### Docker部署

```bash
# 构建后端镜像
./gradlew bootJar
docker build -t blog-backend .

# 运行容器（指定环境）
docker run -d -p 8080:8080 \
  -e SPRING_PROFILES_ACTIVE=prod \
  -v /var/www/blog/uploads:/app/uploads \
  blog-backend
```

### 前端构建

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist` 目录下，可以部署到任何静态服务器。

## 安全说明

1. **JWT Token**：用户登录后会获得JWT Token，存储在localStorage中
2. **路由守卫**：前端使用Vue Router守卫保护管理后台路由
3. **密码加密**：使用BCrypt算法加密存储密码
4. **访问日志**：记录所有访问请求，便于安全审计
5. **CORS配置**：已配置跨域访问控制

## 技术亮点

1. ✅ **Kotlin + Gradle**：使用现代化的Kotlin语言和Gradle Kotlin DSL构建
2. ✅ **JWT认证**：无状态的Token认证机制
3. ✅ **访问日志**：自动记录所有访问请求（拦截器实现）
4. ✅ **路由守卫**：前端路由级别的权限控制
5. ✅ **隐藏后台入口**：首页无任何管理入口，提升安全性
6. ✅ **满屏自我介绍**：现代化的首页设计，滚动查看博客
7. ✅ **响应式设计**：适配各种屏幕尺寸
8. ✅ **Markdown支持**：完整的Markdown编辑和渲染
9. ✅ **图片上传**：支持图片上传和管理
10. ✅ **多管理员支持**：可创建多个管理员账号
11. ✅ **智能图片粘贴**：
    - 剪切板图片自动上传
    - 外部图片链接自动下载替换
    - 图片有效性验证（文件头魔数检测）
    - 前后端双重下载降级机制
12. ✅ **多环境配置**：支持 example/imac/macbook/prod 多环境切换

## 许可证

MIT License
